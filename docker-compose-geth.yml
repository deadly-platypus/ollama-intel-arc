services:
  ollama-intel-arc:
    volumes:
      - /mnt/ollama/ollama-data:/root/.ollama
    networks:
      - llm-internal
  comfyui-ipex:
    volumes:
      - /mnt/ollama/comfyui/app:/app
      - /mnt/ollama/comfyui/python:/usr/local/lib/python3.19
  open-webui:
    volumes:
      - /mnt/ollama/open-webui:/app/backend/data
    networks:
      - traefik_network
      - llm-internal
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.open-webui.rule=Host(`ai-chat.geth.systems`)"
      - "traefik.http.routers.open-webui.entrypoints=websecure"
      - "traefik.http.routers.open-webui.tls=true"
      - "traefik.http.services.open-webui.loadbalancer.server.port=8080"
    
networks:
  traefik_network:
    external: true
  llm-internal:
    external: true
